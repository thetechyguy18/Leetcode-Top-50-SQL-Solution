/* Write your PL/SQL query statement below */
select query_name, Round(AVG(rating/position),2)  quality,
Round(Sum(
    case when rating < 3 then 1 else 0 end
) / count(rating) * 100,2) poor_query_percentage
  from Queries 
group by query_name
