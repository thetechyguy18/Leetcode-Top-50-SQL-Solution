/* Write your PL/SQL query statement below */
select DISTINCT E.EMPLOYEE_ID, e.DEPARTMENT_ID from Employee E
where primary_flag = 'Y' OR E.employee_id IN  
(SELECT EMPLOYEE_ID FROM EMPLOYEE 
GROUP BY EMPLOYEE_ID
HAVING COUNT(*) = 1a
)
