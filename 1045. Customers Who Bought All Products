/* Write your PL/SQL query statement below */
Select c.customer_id from Customer C
group by c.customer_id 
having count(distinct c.product_key) = 
(select count( distinctp.product_key) from Product P)
